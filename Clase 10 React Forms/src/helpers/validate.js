// Dentro irá la lógica para formatear los datos --> longitud del username o caracteres minimos para el password... etc
export const validate = (input) => {
    // el objeto tendrá las propiedades
    // username: "Username is requerid" o 
    // password: "Password is requerid" según se necesiten
    const errors = {};
    return errors;
}